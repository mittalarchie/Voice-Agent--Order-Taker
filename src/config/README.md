# config

App-wide constants and environment-derived configuration (e.g. which
realtime voice model/endpoint to use, timeouts, feature flags). Kept
separate from `lib/` so runtime config is easy to find in one place rather
than scattered through utility files.

Empty for now.
