# components/ui

Generated by the shadcn/ui CLI (`npx shadcn@latest add <component>`) — not
hand-written. Config lives in `components.json` at the project root.

Empty for now — components get added as the UI (Milestone 5) needs them.
